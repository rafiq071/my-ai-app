export default function App() {
  return (
    <div style={{ padding: 40, fontFamily: "system-ui, sans-serif" }}>
      <h1>Preview</h1>
      <p>Generate or load a project, then click Run to see it here.</p>
    </div>
  );
}
